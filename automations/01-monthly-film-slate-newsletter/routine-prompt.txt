You are executing the monthly film slate newsletter for Good Story Studios.

=== TWO NON-NEGOTIABLE GOALS: COMPLETENESS *and* ACCURACY ===
COMPLETENESS: find EVERY in-window film for ALL 24 studios — never skip a studio, especially the streamers (Apple TV+, Amazon MGM, Netflix) and smaller labels (Focus Features, etc.).
ACCURACY: every release date, plot point, AND distributing studio MUST come from a web result you pulled THIS run — NEVER from memory or assumption. If a fact isn't in your search results, omit it.
These coexist because grounding is done with LIGHTWEIGHT searches (below), not heavy full-page reads, so you have budget to cover every film.

STEP 1 — Date & window: run date "+%Y-%m-%d"; window_end = exactly 3 months later; record today and window_end. month_year_label = the spanned window as 'Month – Month YYYY' (e.g. 'June – September 2026') — use this SAME label for BOTH the {{MONTH_YEAR}} header slot AND the email subject line. display_date = "Generated <Month D, YYYY>".

STEP 2 — Read config files:
- automations/01-monthly-film-slate-newsletter/studios.json
- automations/01-monthly-film-slate-newsletter/recipients.json
- automations/01-monthly-film-slate-newsletter/studio-contacts.json   (the studio PR/marketing point-of-contact database — do NOT research contacts on the web; look them up here only)
- automations/01-monthly-film-slate-newsletter/email-template.html

STEP 3 — DISCOVERY (do this for ALL 24 studios BEFORE any grounding; finish the whole list first).
3A. Traditional studios (all except the 3 streamers): WebFetch the studio's wikipedia_url once; from its upcoming/forthcoming-films table, list every film with a release date in the window. If that fetch fails (404/empty) or has no upcoming-films table, DO NOT skip the studio — fall back to a WebSearch ('<studio name> upcoming films 2026 release dates') and list its in-window films from there before moving on.
3B. Streamers (Netflix Studios, Apple TV+, Amazon MGM Studios): WebSearch the search_hint (e.g. '<platform> original films 2026 release dates') and skim the platform's 'List of <platform> original films (2026)' page; list notable films with an in-window release. NEVER return an empty list for Apple TV+, Amazon MGM, or Netflix without explicitly confirming zero in-window releases — list every notable in-window title (including major star-driven originals) before moving on.
Write the COMPLETE candidate list to /tmp/candidates.txt, one line per film: 'Studio | Title | rough date'. The Studio here is PROVISIONAL — it is just where you found the film and may be wrong; Step 4 verifies it. Do NOT skip any studio. Do NOT write synopses yet. Confirm every one of the 24 studios was checked (note which have zero in-window films). DEDUPE: many of these studios are sub-labels or co-producers of each other (New Line/DC → Warner Bros., Columbia → Sony, Marvel/Lucasfilm/20th Century → Disney, Focus/Illumination/DreamWorks → Universal, Skydance → Paramount, Legendary co-productions, etc.), so the same film often appears on two studios' pages — keep it as ONE candidate; it will be filed once under its verified distributor in Step 4 and must NEVER render twice.

STEP 4 — GROUND every candidate (LIGHTWEIGHT — one search per film, facts from results only, never memory).
For EACH film in /tmp/candidates.txt, run ONE WebSearch: '<title> 2026 film release date studio distributor plot'. From the returned snippets/summary (which draw on Wikipedia, Deadline, Variety, etc.), record three things:
- DATE & STATUS: the film's CURRENT US theatrical/streaming date, using its LATEST title, date, and distributor/platform — if it was retitled, re-dated, or moved to a different distributor/platform, use the most recent facts and NEVER stale pre-change data (e.g. a film once called 'Shiver' now released as 'Thrash' on Netflix). DROP any film whose current date is BEFORE today (already released), was removed from the schedule, or delayed past the window. If an exact day is confirmed within today → window_end, record it with date_status=confirmed. If no exact day is set yet but the film is credibly slated within the window (e.g. 'July 2026', 'Summer 2026'), KEEP it with date_status=anticipated, using the month/window as its date. Only drop as undated when there is no credible in-window timeframe at all.
- DISTRIBUTOR: the studio/distributor named in the results for THIS release. If it differs from the provisional studio you filed the film under in Step 3 (common when one studio shelved a film and another now distributes it — e.g. a former Warner Bros. title released by Ketchup Entertainment), FILE THE FILM UNDER THE VERIFIED DISTRIBUTOR'S BLOCK, not the page it was found on. If the distributor is unclear or sources contradict each other, DROP the film. STREAMING PLATFORMS: a title released on a studio's own streamer (Peacock → Universal/NBCUniversal, Max → Warner Bros., Disney+ → Disney, Paramount+ → Paramount, Hulu) may be filed under that parent, but the synopsis MUST make the streaming platform clear — NEVER present a streaming-only release as a theatrical one.
- PREMISE: 1–2 lines of the actual plot/premise as stated in the results (this is the ONLY basis for the synopsis).
Keep these notes brief and move on — this step is deliberately light so you can ground EVERY film without running low on budget. If a film's results are thin, run at most one more targeted search; if still unverifiable, drop it. HARD RULE: if a date, distributor, or plot point is not in your search results, it does not go in the email. (Cast/character grounding is no longer needed — the newsletter no longer lists actors.)

STEP 5 — For each SURVIVING film, write the SYNOPSIS: exactly 2 plain, relatively short sentences summarizing ONLY the grounded premise from Step 4. No added characters/settings/plot. Do NOT add any creator tie-in, brand-integration idea, actor/casting suggestion, or any other recommendation — those are intentionally removed. The newsletter is now strictly: film + release date + studio + studio contact. (The Good Story team handles all integration/creator-fit ideas themselves.)

STEP 5B — Resolve the STUDIO CONTACT for each studio you will render, from studio-contacts.json (loaded in Step 2). Do NOT research contacts on the web — use ONLY this file. For each studio block: take the studio's key, look it up in label_to_company to get the company, then pick ONE contact from that company's `contacts` using selection_order — (1) any contact with status="confirmed" wins; else (2) the contact with primary=true; else (3) the first role_type="partnerships" contact; else (4) a role_type="publicity" contact. Record the chosen contact's name, title, and email, plus its status (to decide the unverified tag) and whether email is null. If a studio's key is somehow missing from the file, render the contact line as "Contact: not yet in database".

STEP 6 — Assemble the HTML. Use email-template.html as the base; preserve its <head>, <style>, and all inline CSS EXACTLY; replace only {{MONTH_YEAR}}, {{GENERATED_DATE}}, {{STUDIO_BLOCKS}}.
Studio block — h2 studio name, then the STUDIO CONTACT block (Step 5B), then the film entries:
<table class="studio-block" role="presentation" cellpadding="0" cellspacing="0" border="0" width="100%" style="margin-bottom:36px;"><tbody><tr><td><h2 style="margin:0 0 14px 0;font-family:'Nunito',Arial,sans-serif;font-size:20px;font-weight:900;color:#6B90D9;letter-spacing:-0.2px;border-left:5px solid #F2C53D;padding-left:14px;line-height:1.2;">STUDIO NAME</h2>STUDIO_CONTACT_BLOCK FILM_ENTRIES</td></tr></tbody></table>
STUDIO_CONTACT_BLOCK (one per studio, using the contact resolved in Step 5B; the yellow box matches the studio accent):
<table role="presentation" cellpadding="0" cellspacing="0" border="0" width="100%" style="margin:0 0 18px 0;"><tbody><tr><td style="background-color:#FFFBEF;border:1.5px solid #F4E3A6;border-radius:12px;padding:13px 18px;"><p style="margin:0 0 4px 0;font-family:'Nunito',Arial,sans-serif;font-size:11px;font-weight:900;color:#C49A1E;text-transform:uppercase;letter-spacing:0.8px;">Studio Contact ✦</p><p style="margin:0;font-family:'Nunito',Arial,sans-serif;font-size:14px;font-weight:800;color:#2D2D2D;line-height:1.45;">CONTACT_NAME <span style="font-weight:600;color:#7A7E8B;">— CONTACT_TITLE</span></p><p style="margin:4px 0 0 0;font-family:'Nunito',Arial,sans-serif;font-size:13px;font-weight:700;line-height:1.5;">CONTACT_EMAIL_PART</p></td></tr></tbody></table>
CONTACT_EMAIL_PART rules: if the contact's email is present, render <a href="mailto:EMAIL" style="color:#6B90D9;text-decoration:none;">EMAIL</a>; if email is null, render <span style="color:#7A7E8B;font-weight:600;">Email: research via LinkedIn / CRM</span>. THEN, if the contact's status is "speculative", append a single space and this tag: <span style="font-size:11px;font-weight:700;color:#C49A1E;background-color:#FFF3CE;border-radius:10px;padding:2px 8px;">unverified</span>  (omit the tag entirely when status is "confirmed").
Film entry (title + release-date pill + 2-sentence synopsis ONLY — no creator or actor blocks):
<table class="film-entry" role="presentation" cellpadding="0" cellspacing="0" border="0" width="100%" style="margin-bottom:14px;"><tbody><tr><td style="background-color:#FAFBFF;border-radius:16px;padding:18px 24px;border:1.5px solid #EDF2FB;"><table role="presentation" cellpadding="0" cellspacing="0" border="0" width="100%" style="margin-bottom:8px;"><tbody><tr><td style="vertical-align:middle;"><span style="font-family:'Nunito',Arial,sans-serif;font-size:16px;font-weight:800;color:#2D2D2D;line-height:1.3;">FILM TITLE</span></td><td style="text-align:right;vertical-align:middle;white-space:nowrap;padding-left:12px;"><span style="font-family:'Nunito',Arial,sans-serif;font-size:12px;font-weight:700;color:#6B90D9;background-color:#EDF2FB;border-radius:20px;padding:3px 10px;display:inline-block;">RELEASE DATE</span></td></tr></tbody></table><p style="margin:0;font-family:'Nunito',Arial,sans-serif;font-size:14px;font-weight:400;color:#7A7E8B;line-height:1.65;">2-SENTENCE SYNOPSIS</p></td></tr></tbody></table>
RELEASE DATE pill: if date_status=confirmed, render the exact date as 'Month D, YYYY' (e.g. 'August 14, 2026'); if date_status=anticipated, render the month/window label followed by ' (anticipated)' (e.g. 'July 2026 (anticipated)').

INTEGRITY CHECK: before Step 7, scan the assembled HTML for stray or partial tags and any literal tag text that leaked into the visible body (e.g. a stray '/tr>'); fix all of them so only clean, balanced markup remains.

STEP 7 — Write the full HTML to /tmp/email_body.html

STEP 8 — Send via Supabase (CCR blocks direct HTTP; load HTML in a FEW raw dollar-quoted chunks, NOT base64).
a) From recipients.json build a JSON ARRAY STRING of ALL addresses and use it verbatim as to_address. It MUST be a JSON array even for a SINGLE recipient — e.g. ["joshuagabbay1@gmail.com"] (square brackets + double quotes), NEVER a bare joshuagabbay1@gmail.com and NEVER a comma-separated plain string. The send function JSON-parses to_address and only fans out to every recipient when it is a valid JSON array; a bare or comma string collapses into one malformed address as soon as there is more than one recipient.
mcp__claude_ai_Supabase__execute_sql (project_id 'qahbmrequuwfklqsfaex'):
INSERT INTO email_queue (to_address, subject, html_body) VALUES ('<RECIPIENTS_JSON_ARRAY>', 'Good Story Studios — <MONTH_YEAR> Film Slate ✦', '') RETURNING id;   -- example to_address literal: '["joshuagabbay1@gmail.com"]'
b) python3 -c "h=open('/tmp/email_body.html',encoding='utf-8').read(); n=12000; c=[h[i:i+n] for i in range(0,len(h),n)]; [open(f'/tmp/part_{i}.txt','w',encoding='utf-8').write(x) for i,x in enumerate(c)]; print('chunks:',len(c))"
c) For each part in order, ONE execute_sql: UPDATE email_queue SET html_body = html_body || $GSS$<exact contents of /tmp/part_i.txt>$GSS$ WHERE id = QUEUE_ID;
d) Verify: SELECT length(html_body) AS len, right(html_body,15) AS tail FROM email_queue WHERE id = QUEUE_ID;  expect 30000+, tail </html>.
e) SELECT net.http_post(url := 'https://qahbmrequuwfklqsfaex.supabase.co/functions/v1/send-film-slate-email', body := jsonb_build_object('queue_id', QUEUE_ID), headers := '{"Content-Type": "application/json"}'::jsonb) AS request_id;
f) bash: sleep 15
g) SELECT id, sent_at, error FROM email_queue WHERE id = QUEUE_ID;  sent_at set = success.

STEP 9 — Gmail draft: read /tmp/email_body.html, call mcp__claude_ai_Gmail__create_draft with to = the SAME recipients.json list used in Step 8 (as an array, e.g. ['joshuagabbay1@gmail.com']) — do NOT hardcode a single address, subject 'Good Story Studios — <MONTH_YEAR> Film Slate ✦', htmlBody = the ENTIRE HTML. If Gmail is unavailable, note it and continue.

CRITICAL RULES:
- COMPLETENESS: finish discovery for ALL 24 studios (Step 3) and write /tmp/candidates.txt BEFORE grounding; never skip a studio. Apple TV+, Amazon MGM, and Focus Features must be checked every run. If a studio's discovery URL fails, use the Step 3A WebSearch fallback rather than dropping the studio.
- ACCURACY: grounding facts come ONLY from this-run search results, never memory; drop films you can't verify or that left the window.
- STUDIO ACCURACY: a film goes under its VERIFIED current distributor (Step 4 DISTRIBUTOR), NOT merely the studio whose Wikipedia page first listed it; reassign or drop on mismatch. Never list the same film under two studios — keep it once under its verified distributor.
- DATE COVERAGE: anticipated (month-only) in-window films ARE included and tagged '(anticipated)'; only films with no credible in-window timeframe are dropped.
- STUDIO CONTACTS: every rendered studio block has exactly ONE Studio Contact box, populated ONLY from studio-contacts.json (never researched live) and chosen by selection_order. Render name + title + email. A contact whose status is "speculative" MUST carry the 'unverified' tag; a "confirmed" contact MUST NOT. If a contact's email is null, use the "research via LinkedIn / CRM" fallback line.
- NO RECOMMENDATIONS: do NOT add creator tie-ins, brand-integration ideas, actor/casting suggestions, or any other recommendation anywhere in the email. The Good Story team owns that.
- HTML INTEGRITY & FIDELITY: the final email has no stray/partial tags or visible literal tag text; each synopsis references ONLY the film's own grounded premise; streaming-only titles are never presented as theatrical.
- BREVITY: keep every entry tight (synopsis 2 sentences; one Studio Contact box per studio) so the whole email stays well under Gmail's ~102KB clipping limit.
- CURRENCY: use each film's latest title/date/distributor; drop anything already released before today or moved out of the window.
- Grounding is one lightweight WebSearch per film (Step 4) — keep it brief so you can cover every film and never drop one for lack of budget.
- Synopsis summarizes the grounded premise ONLY — no creator/actor/brand recommendations. Studio contacts come ONLY from studio-contacts.json, chosen by selection_order.
(Mechanical steps — template/CSS preservation, to_address, dollar-quoted delivery, Gmail draft — are specified in Steps 6, 8, and 9; follow them there.)
