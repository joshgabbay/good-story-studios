[DRAFT — finalize when the Supabase/HubSpot/Slack connectors and the real recipient/channel exist.
The pipeline, history-sync, delivery, and alias seams are already built and tested (48 tests);
this prompt is the thin cloud-routine wrapper that injects the connectors.]

You are running the monthly YouTube Sponsorship Intelligence digest for Good Story Studios.
The repo is cloned; work in automations/02-youtube-sponsorship-intelligence/. Connectors attached:
Supabase, HubSpot, Slack.

STEP 1 — Inputs: the month (YYYY-MM), the path to this month's sponsored-video CSV, config.json.

STEP 2 — Pull history: create a local SQLite DB and load prior months into it from Supabase
(SELECT month, brand_canonical, brand_display, sponsorship_count FROM brand_monthly_counts) via
pipeline.history_store.pull_prior_counts(conn, fetch_counts). SQLite is the compute; Supabase is
durable storage only.

STEP 3 — Run the pipeline over this month's CSV using the tested pipeline.run.run_month (or the
underlying transform → engine.load_month → engine.rank_and_diff path) against that same SQLite DB.
It aggregates long-form rows, ranks Top-N, computes month-over-month (up/down/same/new + new and
dropped brands), and surfaces top unmatched brands for alias review. First month = baseline. All
counting/ranking is deterministic CODE — never tally yourself.

STEP 4 — Relationships: for the Top-N brands, query HubSpot (MCP) for an existing relationship +
deal stage; pass the result map into the report (the same shape relationships.lookup_relationships
returns: {canonical: {has_relationship, stage}}).

STEP 5 — Narrative: write a 2–3 sentence narrative of what actually moved this month (top spender,
notable risers/new entrants, priority warm relationships). This is the ONLY part you author.

STEP 6 — Render: assemble the HTML from email-template.html via report.render_html (set
show_unmatched per config.audience: internal keeps the alias-review section, external omits it) and
the Slack text via report.render_slack.

STEP 7 — Push history: upsert this month's brand_monthly_counts back to Supabase via
pipeline.history_store.push_month_counts(conn, month, upsert_counts) using the upsert in
sql/supabase_schema.sql.

STEP 8 — Deliver: email the HTML to config.final_recipient (Supabase email_queue + send edge
function, same pattern as the film-slate automation) AND post the Slack summary to
config.slack_channel. FIRST RUN: deliver to the owner only until validated, then flip the config.

STEP 9 — Report which brands were surfaced as unmatched so the operator can fold real ones into
brand_aliases.seed.json (tools/alias_add.py) before next month.
